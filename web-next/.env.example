# Canonical public URL for Open Graph, sitemap, and robots (no trailing slash). Falls back to VERCEL_URL or localhost in dev.
NEXT_PUBLIC_SITE_URL=

# Production: full backend URL (no trailing slash). Dev: leave unset to use same-origin /api (see next.config rewrites).
# If you set this to http://localhost:4000 for local dev, the app still uses /api when opened from a tunnel or non-loopback host so API calls stay same-origin.
NEXT_PUBLIC_API_URL=

# Vercel / server: upstream Express origin for Edge route handlers (e.g. /api/ai/chat) and build-time rewrites. Defaults to 127.0.0.1:4000 if unset.
# In production set to the same Railway URL as NEXT_PUBLIC_API_URL (no trailing slash).
API_PROXY_TARGET=

# Same value as backend JWT_SECRET — required for Edge middleware to verify gametime_token (HS256).
# If unset, /parent/* and /child/* always redirect to /login (fail closed).
JWT_SECRET=

# Parent PIN to return from child mode to parent routes (default 1234 if unset in dev).
PARENT_MODE_PIN=

# Optional: Supabase Realtime for instant child Gamer Hub sync (tasks + RP). If unset, polling only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# If your mirrored `child_profiles` table uses another name in Supabase, set it here (default: child_profiles).
# NEXT_PUBLIC_SUPABASE_CHILD_TABLE=child_profiles
