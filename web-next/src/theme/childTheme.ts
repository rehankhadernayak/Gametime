import styles from "@/components/child-gamer-hub/ChildGamerHub.module.css";

/** Class name for the child Gamer Hub theme wrapper (dark neon + glass tokens). */
export const childTheme = styles.childTheme;
