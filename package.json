{
  "name": "gametime-monorepo",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:live": "playwright test --config=playwright.live.mjs",
    "test:e2e:live:headed": "playwright test --config=playwright.live.mjs --headed",
    "secrets:handover": "node scripts/push-secrets-from-env-secrets.mjs",
    "deploy:aws:ecs": "node scripts/deploy-ecs-from-local-env.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.787.0",
    "@playwright/test": "^1.52.0"
  }
}
